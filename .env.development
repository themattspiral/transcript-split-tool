# local dev uses the vite server proxy to prevent CORS issues
TST_GOOGLE_API_BASE=/google-api
TST_OAUTH_GOOGLE_AUTHORITY_URI=/google-accounts/o/oauth2/v2/auth
TST_OAUTH_GOOGLE_REVOKE_URI=https://oauth2.googleapis.com/revoke
TST_OAUTH_GOOGLE_METADATA_URI=/google-accounts/.well-known/openid-configuration

# local dev redirect is explicitly allowed for dev oauth key
# (note: gets changed to 'https' if dev SSL is enabled, in vite.config.ts)
TST_OAUTH_GOOGLE_REDIRECT_URI=http://localhost:5173/google-oauth2-callback

# put this in .env.development.local (intentionally excluded here)
# TST_OAUTH_GOOGLE_CLIENT_ID=<client_id>